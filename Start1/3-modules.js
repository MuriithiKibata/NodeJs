// Common JS all files are modules by default.
// Modules - encapsulated code only share the minimum.

const {muriithi, secret} = require('./4-names')
const sayHi = require('./5-utlis')
require('./6-mind-grenade')

// sayHi(muriithi)
// sayHi(secret)



